// Title: Access Modifiers -> Private, Protected, Internal, Public
/*
Private -> accessible within the same class.
Protected -> accessible within the same class and its subclasses.
Internal members -> accessible within the same module.
Public -> accessible everywhere.
 */

package cosmiclearn.techbar

fun main(args: Array<String>) {
    // public var empId: Int = 0
    // private var empId: Int = 0
    // protected var empId: Int = 0
    // internal var empId: Int = 0
}
